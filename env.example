MUNKI_DOMAIN=
ACME_EMAIL=
MUNKI_REPO_PATH=/srv/munki/repo
CF_API_TOKEN=
SAMBA_USER=
SAMBA_PASS=
# Optional: sets ClientIdentifier in the Munki preferences payload.
# Leave empty to omit it (Munki will use the machine's serial number).
MUNKI_CLIENT_IDENTIFIER=
